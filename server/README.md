# Servidor - Responsable: Jose Quintero

Tareas iniciales sugeridas:

1. Copiar el archivo `server.cpp` desde el proyecto original y colocarlo en este directorio.
2. Crear `docker/Dockerfile.server` con el contenido que ya validamos.
3. Documentar pruebas básicas realizadas al servidor (logs de validación de tokens).

Recuerda trabajar en la rama `feature/server`, hacer tus commits y abrir el PR correspondiente.

